# Optional isolated worker dependencies for CrewAI.
# Install this in a separate virtual environment/service so the core StoreAI
# RAG runtime (which uses chromadb==0.4.22) remains stable.

crewai>=0.119.0,<0.130.0
chromadb>=0.5.23,<0.6.0
